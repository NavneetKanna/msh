#include <stdio.h>
#include <stdlib.h>
#include <string.h>

void init_commands() 
{
    int commands[] = {108+105};

    for (int i=0; i<strlen(commands); i++) {
        // hash(commands[i]);

    }

}